<Page
    x:Class="UWP_UI.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP_UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    >

    <Border>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            
        <Button Click="TestConnectionButtonClicked"
            HorizontalAlignment="Center" VerticalAlignment="Center"
            >Click here to test connection</Button>
            
            <TextBlock Grid.Row="1">Result :</TextBlock>
            
            <TextBlock Grid.Row="2" x:Name="ConnectionResultTextBlock"/>
        </Grid>
    </Border>
</Page>
